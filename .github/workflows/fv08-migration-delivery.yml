name: FV-08 Migration Delivery

on:
  push:
    paths:
      - 'packages/adapters/**'
      - 'packages/application/**'
      - 'packages/core/**'
      - 'tests/fv07_persistence_uow_test.sh'
      - 'tests/fv08_migration_delivery_test.sh'
      - '.github/workflows/fv07-persistence.yml'
      - '.github/workflows/fv08-migration-delivery.yml'
      - 'scripts/governance_lifecycle_phase.sh'
  pull_request:
    paths:
      - 'packages/adapters/**'
      - 'packages/application/**'
      - 'packages/core/**'
      - 'tests/fv07_persistence_uow_test.sh'
      - 'tests/fv08_migration_delivery_test.sh'
      - '.github/workflows/fv07-persistence.yml'
      - '.github/workflows/fv08-migration-delivery.yml'
      - 'scripts/governance_lifecycle_phase.sh'

permissions:
  contents: read

jobs:
  fv08-migration-delivery:
    name: FV-08 migration and delivery semantics
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Set up Node.js 24 LTS
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version-file: .node-version
      - name: Run FV-08 migration and delivery evidence
        shell: bash
        run: bash tests/fv08_migration_delivery_test.sh
