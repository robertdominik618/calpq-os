name: M09-M12 Execution Readiness

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  readiness:
    name: M09-M12 execution readiness
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Run M09-M12 execution readiness guard
        shell: bash
        run: bash tests/m09_m12_execution_readiness_test.sh
