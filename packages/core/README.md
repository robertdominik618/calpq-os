# @calpq/core

Pure domain values, invariants and deterministic rules. No UI, persistence, network or provider dependency.

FV-01 starts the admitted Core kernel with framework-free semantic primitives only: typed UUIDv7 identifiers, UTC/date values, revision/version values, Actor/Subject references, controlled Jurisdiction values and controlled verification states.
